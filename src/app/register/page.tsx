import Register from "@/components/register";
import Wrapper from "@/layout/Wrapper";

export const metadata = {
   title: "Register Xeco - ICO & Crypto Landing Page React Next js Template",
};
const index = () => {
   return (
      <Wrapper>
         <Register />
      </Wrapper>
   )
}

export default index